# Dino English CRM 二期短期替代方案需求文档（PRD）

> 越南站外呼与数据打通（借道国际化 1V1）
>
> 依据「CRM 与运营平台需求对齐」会议纪要、官方迭代计划及交互式 PRD（`index.html` 迭代计划中「二期 · 短期替代方案」）整理。
>
> **版本定位**：本方案排在**一期与二期之间**，是二期「销售中心（分配 / 跟进 leads + 外呼）」完整能力上线前的**短期过渡方案**。为满足**越南站**当下的外呼需求与「广告 → 付费」的数据可见性，先**借道国际化 1V1 后台**支持；待二期销售中心（外呼 + AI 通话小结 + 自动分配/掉库 + 数据回流）上线后，由 CRM 内部**收敛替代**。

---

## 1. 需求概述

### 1.1 背景

- **业务现状**：越南站已有真实的销售外呼诉求，但 CRM 二期「销售中心 + 外呼」要到 **8/7** 才上线，等待周期过长，越南站需要一个可立即落地的过渡方案。
- **两大痛点**：
  1. **外呼能力缺位**：越南站注册用户（Leads）无法被销售系统化地外呼触达。
  2. **数据链路断裂**：越南站看不到从**广告 → 注册**的销售触达情况，也看不到用户**付费、体验课完成**的情况，无法评估投放到转化的完整效果。
- **过渡思路**：复用集团已有的**国际化 1V1 后台**（已具备外呼能力），把 Dino English 的 Leads 同步过去外呼，并将两侧用户 ID 做关联，打通全链路数据；销售跟进记录先沉淀在现有线下表。

### 1.2 目标

| 目标 | 说明 | 对应方案 |
| --- | --- | --- |
| 让越南站销售**呼得动** | 注册 Leads 同步到国际化 1V1 后台，销售在该后台外呼 | 方案一 |
| 让越南站**看得清**全链路 | Dino 用户 ID 与国际化 1V1 用户 ID 关联，打通「广告 → 注册 → 销售触达 → 体验课完成 → 付费」 | 方案二 |
| 让销售触达**可追溯** | 在现有线下表基础上增加销售跟进记录（外呼 / 沟通记录） | 线下表升级 |

### 1.3 目标用户（内部 B 端角色）

| 角色 | 短期方案核心诉求 | 主要动作 |
| --- | --- | --- |
| 💼 越南站销售 | 拿到注册 Leads、发起外呼、记录跟进 | 在国际化 1V1 后台外呼 + 在线下表记跟进 |
| 📣 市场投放 / 增长 | 看清「广告 → 注册 → 付费」的转化效果 | 查看打通后的全链路数据 |
| 🛠️ 运营 / 数据 | 维护线下表、核对同步与关联数据 | 导表 / 关联核对 |

### 1.4 上线目标与依赖

- **上线定位**：短期过渡（一期与二期之间），越南站先行；无独立复杂系统建设，以链路打通 + 线下表升级为主。
- **关键依赖**：
  - 国际化 1V1 后台的 Leads 接收能力与外呼能力（已具备，需确认接口 / 导入方式）。
  - Dino English 注册成功事件的 Leads 同步出口。
  - Dino English 与国际化 1V1 双侧的用户 ID 映射关系与打通字段（付费 / 体验课完成回传）。
- **收敛条件**：二期销售中心上线后，外呼、跟进记录、数据回流由 CRM 内部承接，本方案下线。

---

## 2. 方案一 · 销售外呼能力（Leads 同步链路）

### 2.1 目标
为满足越南站的外呼需求：用户在 **Dino English** 注册成功后，把 **Leads 同步到国际化 1V1 后台**，支持销售在国际化 1V1 后台上进行外呼。

### 2.2 链路

```
用户 →（注册成功）Dino English →（同步 Leads）国际化 1V1 后台 →（外呼）销售
```

### 2.3 同步规则

| 项 | 规则 |
| --- | --- |
| 触发时机 | Dino English **注册成功**后触发同步 |
| 同步对象 | 越南站注册用户的 Leads（建议对齐二期口径：**有手机号**的用户才可外呼；无手机号仅留档） |
| 同步方向 | Dino English → 国际化 1V1 后台（单向推送） |
| 同步内容 | 用户标识、姓名、手机号、注册时间、渠道来源、国家等外呼所需基础信息 |
| 外呼执行 | 销售在**国际化 1V1 后台**发起外呼（复用其现有外呼能力） |

### 2.4 功能需求清单（方案一）

| 功能点 | 说明 | 优先级 |
| --- | --- | --- |
| 注册事件触发同步 | Dino 注册成功后自动向国际化 1V1 后台同步 Leads | P0 |
| Leads 字段映射 | 定义 Dino → 国际化 1V1 的字段映射（手机号 / 姓名 / 渠道等） | P0 |
| 手机号可外呼判定 | 有手机号才进入外呼池（对齐二期规则） | P0 |
| 同步幂等 / 去重 | 同一用户重复注册 / 重试不产生重复 Leads | P1 |
| 同步失败重试 / 告警 | 同步失败可重试并可观测 | P1 |

---

## 3. 方案二 · 全链路数据打通（用户 ID 关联）

### 3.1 痛点
越南站无法看到从**广告到注册**的用户销售触达情况，也看不到用户**付费、体验课完成**的情况。

### 3.2 方案
将 **Dino English 用户 ID 与国际化 1V1 用户 ID 做关联**，打通完整用户旅程：

```
广告 → 注册 → 销售触达 → 体验课完成 → 付费
```

### 3.3 关联与数据口径

| 环节 | 数据来源 | 打通后可见 |
| --- | --- | --- |
| 广告 | 渠道 / 投放归因 | 用户从哪个广告 / 渠道而来 |
| 注册 | Dino English | 注册用户与其渠道来源 |
| 销售触达 | 国际化 1V1 后台外呼 + 线下表跟进 | 该用户是否被外呼 / 跟进及结果 |
| 体验课完成 | Dino English 业务数据 | 是否完成体验课 |
| 付费 | Dino English 订单 / 支付 | 是否付费、付费金额 |

> **关联键**：以 Dino 用户 ID ↔ 国际化 1V1 用户 ID 的映射为主键，保证同一用户在两侧可对齐。

### 3.4 功能需求清单（方案二）

| 功能点 | 说明 | 优先级 |
| --- | --- | --- |
| 用户 ID 关联 | 建立 Dino ↔ 国际化 1V1 用户 ID 映射表 | P0 |
| 广告→注册归因可见 | 打通渠道来源与注册数据 | P0 |
| 销售触达可见 | 外呼 / 跟进结果可回看到具体用户 | P1 |
| 体验课完成回传 | 体验课完成状态可对齐到用户 | P1 |
| 付费回传 | 付费状态 / 金额可对齐到用户 | P0 |
| 全链路报表口径 | 支持「广告 → 注册 → 触达 → 体验 → 付费」漏斗查看 | P1 |

---

## 4. 线下表升级 · 增加销售跟进记录

### 4.1 目标
在**现有线下表**的基础上，再加上**销售的跟进记录**（外呼 / 沟通记录），让销售触达过程可追溯并沉淀。

### 4.2 线下表新增字段

在现有线下表基础上，**重点新增以下三列**用于沉淀销售外呼跟进：

| 新增列 | 取值 / 说明 |
| --- | --- |
| 最近一次接通状态 | **未拨打 / 已接通 / 未接通 / 已拒绝** |
| 拨打次数 | 累计外呼拨打次数 |
| 最近一次跟进记录 | 最近一次外呼 / 沟通的跟进内容小结 |

其余建议配套字段（按需）：

| 字段 | 说明 |
| --- | --- |
| 跟进销售 | 负责该 Lead 的销售 |
| 跟进时间 | 每次外呼 / 沟通的时间 |
| 触达方式 | 外呼 / 其他沟通渠道 |
| 跟进进度 | 待跟进 / 跟进中 / 已付费 / 暂不跟进（对齐二期进度口径） |
| 用户关联 ID | Dino 用户 ID（便于与方案二数据对齐） |

> 线下表为过渡载体，字段口径尽量对齐二期销售中心的跟进记录字段，便于后续迁移收敛。

### 4.3 功能需求清单（线下表）

| 功能点 | 说明 | 优先级 |
| --- | --- | --- |
| 跟进记录字段扩展 | 现有线下表新增三列（最近一次接通状态 / 拨打次数 / 最近一次跟进记录）+ 配套字段 | P0 |
| 跟进进度口径对齐 | 进度枚举对齐二期，便于收敛迁移 | P1 |
| 与用户 ID 对齐 | 跟进记录带 Dino 用户 ID，可与全链路数据关联 | P1 |

---

## 5. 与二期的关系（收敛路径）

本方案是二期「销售中心 + 外呼」正式上线前的**短期替代方案**：

- **方案一** 先打通「**注册 → 外呼**」执行链路；
- **方案二** 先打通「**广告 → 付费**」数据链路；
- **销售跟进记录** 先沉淀在线下表。

待二期销售中心（**外呼 + AI 通话小结 + 自动分配 / 掉库 + 数据回流**）上线后，由 **CRM 内部收敛替代**。

| 短期能力（本方案） | 承载方 | 二期收敛后（目标态） | 承载方 |
| --- | --- | --- | --- |
| Leads 外呼 | 国际化 1V1 后台 | 销售中心点号码拨打 + 录音 + AI 小结 | CRM |
| 销售跟进记录 | 线下表 | 跟进记录时间轴 + 自动归档 | CRM |
| 线索分配 | 人工 / 线下 | 组长按权重自动分配 / 超时掉库 / 重分配 | CRM |
| 全链路数据 | 用户 ID 关联 + 报表 | 用户中心精准归因 + 付费回流 | CRM |

---

## 6. 待确认事项（Open Issues）

1. **国际化 1V1 后台对接方式**：Leads 是走接口实时推送，还是定时导表 / 批量导入？外呼能力是否需要额外配置越南站坐席。
2. **用户 ID 映射建立时机**：注册即建立映射，还是首次外呼 / 首次同步时建立？历史用户是否需要回补映射。
3. **付费 / 体验课完成回传链路**：由 Dino 主动推送还是国际化 1V1 侧拉取？回传延迟与一致性要求。
4. **线下表责任人与维护频率**：线下表由谁维护、更新频率，如何保证与系统数据一致。
5. **收敛切换方案**：二期销售中心上线时，线下表跟进记录与外呼历史是否需要迁移进 CRM，迁移口径与时间点。
6. **数据合规**：越南站用户手机号 / 个人信息跨系统同步的合规与安全要求。

---

## 7. 关联资源

- 迭代计划与交互式 PRD：`index.html`（「二期 · 短期替代方案」）
- 二期正式方案：`CRM-Phase2-PRD.md`（销售中心 + 系统配置·角色权限）
- 原型仓库：https://github.com/jiangshuang12345-tech/CRM.git
- 在线原型：https://jiangshuang12345-tech.github.io/CRM/
