# Dino English CRM 第二期需求文档（PRD）

> 依据「CRM 与运营平台需求对齐」会议纪要、官方迭代计划及 CRM 原型仓库（`SalesCenter.tsx` / `SystemConfig.tsx` 及配套逻辑）整理。原型在线预览：https://jiangshuang12345-tech.github.io/CRM/
>
> **版本对照**：本期即官方迭代计划中的「二期」，计划于 **8 月 18 日上线**，包含两大模块：**销售中心**（分配 / 跟进 leads + 接入外呼）与**系统配置·角色权限**（RBAC + 数据隔离 + 操作日志）。

---

## 1. 需求概述

### 1.1 背景与目标
一期已完成「用户中心 / 订单中心」的数据可查（看得清）。二期的核心目标是让业务能够在 **CRM 内完成 leads 分配与外呼的完整流程**，减少跨系统操作、提高销售效率；角色权限则保障各角色在授权范围内高效协作：

- **销售中心**：承接注册线索的**自动沉淀 → 按业务线权重自动分配 → 人工跟进转化**，并**接入外呼系统**（点号码拨打 + 通话录音 + AI 通话小结 + 自动归档），形成「注册 → 分配 → 跟进/外呼 → 付费回流用户中心」的转化闭环；线索按业务线自动分配、超时无跟进自动掉库重分、超 30 天未付费自动退回公海，销售/组长可手动退回公海、重新分配。
- **系统配置·角色权限**：建立 RBAC（角色-权限）体系，权限细分到子权限（无 / 只读 / 可操作），配合业务线数据隔离与操作日志，支撑多角色、多业务线安全协作。
- **用户中心·添加会员（补充）**：在一期用户中心基础上补充「添加会员 / 加有效期」写操作（替代找产研改库），按付费状态约束有效期选择，纳入二期 RBAC 与操作日志（详见模块三）。

> **数据权限内置业务线（8 条）**：马来（MYR/+60）、泰国（THB/+66）、印尼（IDR/+62）、新加坡（SGD/+65）、越南（VND/+84）、韩国（KRW/+82）、沙特（SAR/+966）、其他（USD/+1，catch-all）。

### 1.2 目标用户（内部 B 端角色）
| 角色 | 二期核心诉求 | 高频模块 |
| --- | --- | --- |
| 💼 销售专员 | 领取「未付费-未体验+有手机号」线索、记录跟进、外呼促成付费 | 销售中心（待领取 / 我的跟进 / 通话记录） |
| 👔 销售组长（Leader） | 配置自动分配比例 / 掉库规则、重新分配线索、查看组内全部跟进 | 销售中心（含设置） |
| 🎧 客服 / 运营 | 辅助查看与跟进 | 销售中心（只读/操作，视权限） |
| ⚙️ 管理员 / 系统配置 | 配置角色权限矩阵、成员账号与数据范围、审计操作日志 | 系统配置·角色权限 |

### 1.3 上线计划与依赖
- **上线计划**：8 月 18 日。
- **关键依赖**：外呼接入方案（第三方外呼平台对接方式、录音回传、AI 小结生成能力）。

---

## 2. 模块一：销售中心

### 2.1 业务流转（漏斗口径）
销售中心**仅处理具备触达条件（有手机号）且尚未付费未体验**的潜在客户线索。

```
用户注册 → 有手机号 自动进入销售中心 → 按业务线权重自动分配（或进入待领取）
        → 跟进中 →（付费）回流用户中心
        → 超时无跟进 自动掉库重分 ／ 超 30 天未付费 自动退回公海
```

| 场景 | 归属 | 说明 |
| --- | --- | --- |
| **注册（未付费-未体验）+ 有手机号** | **销售中心线索** | 自动进入销售中心；若命中业务线自动分配规则则直接分配到销售（进度「跟进中」），否则进入「待领取」线索池 |
| 注册 + 无手机号（谷歌邮箱 / Facebook / AppID 第三方登录） | 用户中心 | 拿不到手机号，无法电话跟进，仅在用户中心留档 |
| 已体验 / 已付费 / 付费逾期 | 用户中心 | 全部在用户中心管理 |
| 用户完成付费 | **回流用户中心** | 用户状态变为「付费」，不再满足销售线索口径，自动离开销售中心 |

> **手机号规则**：仅 `kakao` / `手机号` 登录方式带手机号；`谷歌邮箱` / `Facebook` / `AppID` 无手机号。**不支持手动录入手机号**，线索来源全部由系统自动判定沉淀。

#### 自动分配 / 掉库规则（模拟后端，数据加载或修改设置时触发）
1. **超 30 天未付费自动退回公海（仅一次）**：线索注册超过 30 天仍未付费，自动清除归属、退回「待领取」，且**不再自动分配**（需销售手动领取）；写入系统跟进记录「30天内未付费，自动掉回公海」。此规则**每条线索仅触发一次**（历史已有该记录则不再重复触发），避免被销售手动捞取后又被反复踢回。
2. **超时无跟进自动掉库重分**：按业务线设置开启自动掉库时，领取后**超过 N 分钟无新跟进记录**，自动掉库并按权重重新分配；写入系统记录「超过 N 分钟无跟进」。**注册超 30 天的老线索被重新捞取后免疫此规则**（仅支持手动退回公海）。
3. **新线索/掉库线索自动分配**：注册未满 30 天且当前无归属的线索，按该业务线的分配权重随机分配给相关销售（权重 0 不参与）；分配后进度置「跟进中」，写入系统记录「系统自动分配」。无有效权重配置时线索停留在「待领取」。

> 上述三条规则以「系统」为操作人写入跟进记录，便于追溯。落地由后端定时任务实现。

### 2.2 跟进进度定义
| 进度 | 含义 | 标签色 |
| --- | --- | --- |
| **待领取** | 线索已进入销售中心，尚无销售认领 | 橙 |
| **跟进中** | 已被销售领取，正在联系与转化 | 蓝 |
| **暂不跟进** | 暂时搁置（如家长仍在观望）；仍归属该销售 | 灰 |

> 销售中心跟进进度枚举为：待领取 / 跟进中 / 暂不跟进。**「已付费」不是一个手动进度**——线索一旦完成付费，用户状态变为「付费」，即不再满足销售线索口径而自动离开销售中心（回流用户中心）。跟进记录时间轴中若出现「已付费」为转化留痕，配绿色标签。

### 2.3 页面结构：三个 Tab

顶部为流程说明横幅（可展开「查看详情」）+ 全局筛选区（业务线/国家筛选 `LineFilter` + 关键字搜索 +「通话记录」Tab 额外提供通话结果筛选）。页面右上角在有 `sales_config` 权限时显示「设置」入口。**页面默认停留在「我的跟进」Tab**。

> **筛选器数据权限**：数据范围受限（`line`）的账号，业务线/国家筛选器锁定为其可见业务线（`disabled`），不可越权切换。

#### Tab 1 · 我的跟进（follow，默认）
- 展示本人已领取线索（`isClaimedLead`）；**超管 / 拥有重新分配权限的组长可见范围内全部领取记录**（`seeAllOwners`）。
- 组长身份（拥有 `sales_config`）在此 Tab 顶部展示提示横幅：「你是销售组长，可查看并重新分配组内（业务线范围）全部销售的跟进线索。」
- 支持关键字搜索 + 业务线筛选。
- 相较待领取列表，额外列：**最新备注 / 最后更新时间 / 领取人**。
- 操作列（按权限组合展示）：
  - **外呼**（`sales_dial`）：无手机号时按钮禁用。
  - **更新跟进**（`sales_update`）。
  - **重新分配**（`sales_reassign`）：仅组长/超管。
  - **退回公海**（`sales_update`，danger 样式）：手动将线索退回待领取（见交互流程 ⑤）。

#### Tab 2 · 待领取注册用户（pool）
- 展示尚无领取人的线索（`isPoolLead`：是销售线索且 `salesOwner` 为空）。
- Tab 标题带数量：`待领取注册用户 (N)`。
- 支持关键字搜索（手机号 / 用户ID / 姓名 / 国家）+ 业务线筛选。
- 操作列：**领取跟进**（需 `sales_claim` 权限）。点击后线索 `salesOwner` 置为当前用户、进度置「跟进中」，写入一条跟进记录（备注：「销售已领取，开始跟进已注册用户」），自动跳转到「我的跟进」。

#### Tab 3 · 通话记录（calls）
- 集中查看历史外呼记录；Tab 标题带数量。
- 顶部横幅：「外呼系统与 CRM 轻量集成：坐席点击号码发起外呼，挂断后填写通话小结，系统自动归档到客户档案与销售跟进记录。」
- 支持关键字（手机号 / 用户ID / 客户）+ 通话结果筛选（已接通 / 无人接听）+ 业务线筛选。
- **数据隔离**：非超管（无重新分配权限）仅见本人坐席（`agent`）的记录。

页面底部展示统计提示：当前销售中心共 N 条线索。

### 2.4 关键交互流程

**① 领取线索**
在「待领取注册用户」Tab 按关键字/业务线筛选，点「领取跟进」认领 → 自动进入「我的跟进」，进度置「跟进中」。

**② 常规跟进（更新跟进弹窗）**
在「我的跟进」点「更新跟进」→ 弹窗展示手机号、领取人（只读）+ 本次备注（必填）；底部为**跟进记录时间轴**（进度 · 备注 · 时间 · 领取人，含外呼录音播放条）。保存后写入一条新的跟进记录快照（沿用当前进度）。

**③ 外呼跟进（外呼弹窗，两阶段）**
1. **呼叫阶段**：弹窗显示客户姓名 + 业务线 + 手机号，「通话中」状态并实时计时（`mm:ss`）；按钮为「挂断并填写小结」。
2. **小结阶段**：挂断后进入小结表单——
   - 通话结果：已接通 / 无人接听（无人接听时长记为「—」）。
   - 通话时长：自动带入（只读）。
   - **通话小结（必填）**：接通且时长>0 时，系统模拟 **AI 自动总结**（约 1.5s 生成，展示「AI正在为您总结通话记录...」，生成文案可编辑）。AI 小结的提示词与输出规范见 **2.9**。
   - **购买意向（下拉）**：可选「未填写 / 有意向 / 无意向」，默认「未填写」。保存后**同步更新该线索列表的「购买意向」列**（有意向绿色、无意向红色、未填写灰色横线）。
   - 保存后：生成一条通话记录（`CallRecord`），并**自动归档到该线索的销售跟进记录**（备注前缀「【外呼自动记录】」）；接通的通话附带一条**录音链接**，可在跟进记录时间轴内播放。

**④ 重新分配（组长/超管）**
在「我的跟进」点「重新分配」→ 弹窗显示当前领取人 + 选择新销售（下拉为可分配销售，排除当前领取人）→ 确认后改写 `salesOwner`，并归档一条跟进记录（备注：「重新分配给 XXX」）。

**⑤ 退回公海（手动掉库）**
在「我的跟进」点「退回公海」→ 弹窗**必填「退回原因」**→ 确认后清除归属（`salesOwner` 置空）、进度回「待领取」，并归档一条跟进记录（备注前缀「【手动退回公海】」+ 原因）。线索回到待领取池，供他人重新领取或按规则重新分配。

**⑥ 转化闭环**
线索完成付费后，用户状态变为「付费」，即不再满足销售线索口径，**自动**离开销售中心，在用户中心可查（由真实付费事件驱动，非手动置位）。

### 2.5 分配与掉库设置（按业务线独立配置）
入口：页面右上角「设置」（弹窗标题「分配与掉库设置」，需 `sales_config` 权限）。

- **按业务线配置**：弹窗顶部「所属业务线」下拉，切换后分别维护该业务线的掉库规则与分配比例；切换时先校验并暂存当前业务线表单，再回显目标业务线配置。可配置的业务线范围 = 账号数据权限范围（超管为全部业务线）。
- **超时掉库规则**：开关（**默认关闭**）+「领取后超过 `N` 分钟无跟进记录，自动掉库并重新按比例分配」（`N` 默认 **1440 分钟（=24 小时）**，范围 1–43200 分钟（≈30 天））。
- **线索分配比例**：以表格列出该业务线**可接线的销售**（销售 + Email + 权重），权重范围 0–100（**填 0 表示不分配**）。线索自动分配或掉库重分配时，按权重随机分配给相关销售。
- **可被分配/接线的销售**：账号状态为「启用」、**非系统管理员（`role_admin`）**、其角色销售模块（`sales`）权限为「可操作」；分配比例表按销售的业务线归属（`businessLines`）进一步过滤到当前业务线。
- 保存设置会触发一次「模拟后端」的掉库与自动分配重算。

### 2.6 列表字段（数据字典）

**待领取 / 我的跟进列表（复用用户中心字段）**
| 字段 | 说明 |
| --- | --- |
| 用户ID | 复用用户中心字段（固定左列） |
| 姓名 | 本地名优先，无则英文名 |
| 购买意向 | 未填写 / 有意向 / 无意向；由外呼小结弹窗的「购买意向」下拉写入。**未填写**（默认）灰色横线标签、**有意向**绿色标签、**无意向**红色标签 |
| 用户状态 | 未付费-未体验 / 未付费-体验中 / 未付费-已体验 / 付费 / 付费逾期（按课时记录动态判定） |
| 用户类型 | 正式用户 / 测试用户（按手机号规则自动识别，剔除测试线索） |
| 年龄段 | 3-5 / 6-8 / 9-12 / 13-17 / 18+ |
| 登录账号 | 邮箱 / FB / kakao ID / AppID / 手机号 |
| 渠道来源-落地页 | 落地页投放归因文案 |
| 渠道 code | 落地页渠道 code |
| 渠道来源-App | 三方归因（投放渠道 / 子渠道） |
| 国家 | 注册国家（`LineFilter` 按此筛选，展示业务线/国家标签） |
| 注册时间 | 底层 UTC，按注册国家换算当地时间并标注时区（`LocalTime`） |
| 最新备注 / 最后更新 / 领取人 | 仅「我的跟进」展示，为最近一次跟进快照 |

> 本次调整：销售中心列表**移除「业务线」列与「到期时间」列**（业务线信息合并到「国家」列与顶部筛选器体现，销售场景不关注到期时间）。

**通话记录字段（`CallRecord`）**
| 字段 | 说明 |
| --- | --- |
| 起呼时间 | 发起外呼的时间 |
| 客户 / 手机号 | 被呼叫线索的客户姓名、号码 |
| 通话结果 | 已接通 / 无人接听 |
| 时长 | `mm:ss`；无人接听为「—」 |
| 本次跟进记录 | 坐席填写的通话小结（必填），同步写入销售跟进记录 |
| 外呼坐席 | 发起外呼的操作人 |

> 通话记录列表**移除「业务线」列**（`businessLine` 仍存于数据中，用于业务线筛选与数据隔离，仅不作为独立展示列）。

### 2.7 功能需求清单（销售中心）
| 功能点 | 说明 | 权限点 | 优先级 |
| --- | --- | --- | --- |
| 线索自动沉淀 | 「注册 + 有手机号 + 未付费未体验」自动进入销售中心；不支持手动录入手机号 | — | P0 |
| 自动分配 | 新线索/掉库线索按业务线权重随机分配（<30 天、权重>0），进度置「跟进中」 | — | P0 |
| 我的跟进列表 + 筛选 | 本人线索（组长/超管见全部）+ 关键字/业务线；受限账号筛选器锁定 | — | P0 |
| 待领取列表 + 筛选 | 关键字 + 业务线筛选 | — | P0 |
| 领取跟进 | 认领为本人线索，进度置「跟进中」，进入我的跟进 | `sales_claim` | P0 |
| 更新跟进 | 备注（必填）→ 写入跟进记录时间轴 | `sales_update` | P0 |
| 退回公海（手动掉库） | 填必填原因 → 清除归属、回「待领取」、归档记录 | `sales_update` | P0 |
| 跟进记录时间轴 | 进度 · 备注 · 时间 · 领取人（含录音播放，含系统自动记录） | `sales_update` | P1 |
| 外呼（点号码拨打） | 无手机号不可外呼；呼叫→挂断→填小结 | `sales_dial` | P1 |
| AI 通话小结 | 接通后自动生成小结，可编辑 | `sales_dial` | P1 |
| 购买意向标记 | 外呼小结弹窗下拉选「未填写/有意向/无意向」，保存后同步列表「购买意向」列（绿/红/灰标签） | `sales_dial` | P1 |
| 通话录音 | 接通通话附录音链接，跟进记录内可播放 | `sales_dial` | P1 |
| 通话自动归档 | 生成通话记录 + 归档到跟进记录（前缀「【外呼自动记录】」） | `sales_dial` | P1 |
| 通话记录 Tab | 关键字 + 通话结果筛选；非超管仅见本人坐席记录 | — | P1 |
| 重新分配 | 改写归属销售 + 归档记录 | `sales_reassign` | P1 |
| 设置：超时掉库（按业务线） | 超过 N 分钟无跟进自动掉库重分（默认关闭，N 默认 1440 分钟） | `sales_config` | P1 |
| 设置：分配比例（按业务线） | 按权重自动/掉库重分配（0=不分配） | `sales_config` | P1 |
| 30 天自动退回公海 | 注册超 30 天未付费自动退回待领取，仅触发一次；被捞取后免疫自动掉库，仅手动退回 | — | P1 |
| 转化闭环 | 用户完成付费 → 状态变「付费」→ 自动离开销售中心，回流用户中心 | — | P0 |
| 数据隔离 | 按业务线；销售仅见本人线索/坐席记录 | — | P0 |
| 历史数据刷库（上线初始化） | 按业务提供的「手机号→销售邮箱」映射表，批量把存量用户绑定到对应销售名下 | — | P0 |

### 2.8 历史数据刷库（上线初始化）

**背景**：销售中心上线前，销售在「外呼公司平台 + Lark 表」线下跟进存量线索。上线后需要把这批**已在人工跟进的存量用户**一次性绑定到 CRM 内对应的销售名下，避免线索重新落入「待领取」被打散或重复分配。

**流程**
1. **业务方**：整理并提供「**手机号 → 销售邮箱**」对应关系表（存量线索当前实际归属）。
2. **研发**：以该表为输入，编写一次性刷库脚本，按手机号匹配 CRM 中的用户，将命中用户的 `salesOwner` 绑定到对应销售邮箱、进度置「跟进中」，并写入一条初始化跟进记录（建议备注前缀「【历史数据初始化】」，`owner=系统`），使其上线即出现在该销售的「我的跟进」中。

**导入表字段（业务方提供）**
| 字段 | 必填 | 说明 |
| --- | --- | --- |
| 手机号 | 是 | 唯一匹配键，需含国家码（如 `+82 10-xxxx-xxxx`），格式与库内一致 |
| 销售邮箱 | 是 | 目标归属销售，须为 CRM 内**已存在、启用、具备销售操作权限**的账号（`@dinoai.ai`） |
| 业务线 | 否 | 用于核对/多号归属校验（可选，便于比对渠道推导的业务线） |
| 备注 | 否 | 迁移说明（可选，写入初始化跟进记录） |

**匹配与校验规则**
- **仅绑定符合销售线索口径的用户**（有手机号、未付费-未体验）；已付费 / 已体验的用户不在销售中心，跳过。
- **手机号唯一匹配**：以（含国家码规整后的）手机号为键；一号对一销售。
- **销售邮箱有效性**：目标销售不存在 / 已停用 / 无销售操作权限 → 该行标记失败，不绑定。
- **数据隔离一致性**：绑定的销售业务线范围应覆盖该用户业务线；不一致行需业务确认（校验告警）。
- **幂等**：脚本可重复执行，已正确绑定的记录不重复写入初始化跟进记录。

**异常与产出**
- 未匹配到用户 / 号码格式不符 / 销售无效 / 重复号码 → 输出**失败明细清单**（行号 · 手机号 · 失败原因）返回业务方修订后重跑。
- 刷库为**一次性上线初始化操作**，完成后新增线索走「自动分配 / 待领取」常规流程。

> 待确认：手机号规整口径（国家码/空格/分隔符统一规则）、一个手机号对应多个用户时的处理、绑定后是否发通知给销售。

### 2.9 AI 外呼小结 Prompt（研发参考）

外呼挂断后，系统将通话录音的 **ASR 语音转写文本**送入大模型，自动生成结构化通话小结回填到「通话小结」输入框（坐席可再编辑）。以下为建议的系统提示词与输出规范，供研发接入大模型时参考。

**System Prompt / 系统提示词**

```
你是一个专业的教育行业电销助理。请根据提供的外呼通话语音转写文本（ASR），
提取最核心的业务信息，并二次生成一段清晰、精准的通话小结。

【输出要求】
1. 字数限制：严格控制在 100 字以内。
2. 信息量：必须包含以下三点：
   - 客户意向：明确指出（高意向 / 中意向 / 低意向 / 无意向）。
   - 核心关注点：客户在电话中主要询问或关心的问题（如：价格、外教资质、
     上课时间、退费政策等）。
   - 下一步安排：明确后续动作（如：已加微信发资料、约定下周二再跟进、
     无意向暂搁置、需邀约参加体验课等）。
3. 语言风格：客观、精炼、直接输出总结结果，不要任何"好的"、"总结如下"等语气词。

【输出格式示例】
【意向中】家长关注外教口音和上课时间，已同意添加微信便于了解详情，
建议本周五下发体验课邀约后续再跟进。
```

**要点说明**
- **控制字数与信息量**：录音转写文本冗长，限制 100 字并规定"意向 / 关注点 / 下一步"三要素，确保小结结构统一、可快速阅读。
- **结果直出**：要求模型直接输出总结、不带寒暄语气词，便于直接写入跟进记录。
- **落地衔接**：生成结果写入通话小结输入框，坐席确认/修改后保存 → 生成通话记录并归档到销售跟进记录（前缀「【外呼自动记录】」，见 2.4 ③）。

> 待确认：大模型选型与调用方式（是否实时流式）、ASR 供应商与多语言（韩/阿/越/泰/印尼等）转写质量、意向标签口径是否与销售跟进进度打通。

---

## 3. 模块二：系统配置·角色权限

菜单标题「系统配置 · 角色权限」，含三个 Tab：**角色权限 / 成员账号 / 操作日志**。

### 3.1 权限模型（RBAC）

**权限级别（`PermLevel`）**
| 级别 | 含义 | 效果 |
| --- | --- | --- |
| `none` 无权限 | 菜单隐藏 | 不可见 |
| `view` 只读 | 查看 / 导出 | 只读 |
| `operate` 可操作 | 查看 + 新增 / 编辑 | 可操作 |

**数据权限（`DataScope`）**
| 范围 | 含义 |
| --- | --- |
| `all` 全部业务线 | 可见全部业务线数据 |
| `line` 指定业务线 | 仅可见分配到的业务线数据（业务线可选项含 **「其他」**，用于覆盖无渠道/无法归类的 App 用户，判定口径见 5.3） |

**权限矩阵：主模块 + 子权限（细分到子权限）**
| 主模块 | 子权限 |
| --- | --- |
| 用户中心（users） | 修改用户（users_edit）、添加会员/加有效期（users_add_member） |
| 销售中心（sales） | 领取（sales_claim）、外呼（sales_dial）、更新跟进（sales_update）、重新分配（sales_reassign）、销售设置（sales_config） |
| 订单中心（orders） | —（`orders` 不提供「可操作」，仅 无/只读） |
| 系统配置（system） | 新增角色（system_role_add）、编辑角色（system_role_edit）、删除角色（system_role_delete）、新增成员（system_acc_add）、编辑成员（system_acc_edit） |

> 权限矩阵仅覆盖二期上线模块（用户中心 / 销售中心 / 订单中心 / 系统配置）。营销中心（渠道 / 落地页 / 商品包 / 优惠券）等后续模块待其对应期次上线时再纳入。

**权限联动规则**
- 主模块权限切换为**非「可操作」**时，其下所有子权限自动重置为 `none`。
- 编辑角色时，子权限行仅在其父模块为「可操作」时才展示可配置。
- 子权限只有 无 / 可操作 两档；主模块有 无 / 只读 / 可操作 三档（`orders` 无「可操作」）。

### 3.2 Tab 1 · 角色权限
- **角色列表**：角色名（内置角色带「内置」标签、后期角色带「预定」标签）、职责描述、数据范围（全部业务线 / 指定业务线）、成员数、操作（编辑权限 / 删除）。
  - 内置角色不可删除；被成员占用的角色不可删除（提示「有 N 个成员正在使用」）。
- **权限矩阵表**：行=模块（主模块 + 子模块平铺缩进），列=各角色，单元格显示该角色对该模块的权限级别标签。图例：可操作 = 查看+新增/编辑；只读 = 查看/导出；无权限 = 菜单隐藏。
- **新增 / 编辑角色弹窗**：角色名称（必填，≤20 字）、职责描述、数据范围（单选：全部业务线 / 指定业务线）、权限配置表（逐模块选 无/只读/可操作，子权限随父模块联动）。保存写入操作日志。

**内置角色（种子数据）**
| 角色 | 数据范围 | 说明 |
| --- | --- | --- |
| 超级管理员（默认兜底） | 全部业务线 | 未匹配到账号角色时按超管处理，全模块可操作 |
| 管理员 / 系统配置 | 全部业务线 | 账号鉴权、业务线/模版、角色权限 |
| 运营 / 商业化 | 指定业务线 | 用户中心、订单中心查询（后期扩展运营配置） |
| 客服 / 用户支持 | 指定业务线 | 用户中心、订单中心 |
| 销售组长 | 指定业务线 | 管理销售团队线索分配与掉库规则 |

> 注：市场投放 / 增长（营销中心为主职能）、学习服务 / 班主任（随服务节点引入）等角色，随其对应期次上线时再纳入内置角色。

### 3.3 Tab 2 · 成员账号
- **列表字段**：姓名、登录邮箱、角色、数据范围（角色为 `all` 显示「全部业务线」；`line` 显示已分配业务线标签，未分配显示「—」）、状态、最近登录、**坐席相关列（见 3.3.1）**、操作。
- **操作列**：`停用/启用`、`编辑`、`绑定外呼/解绑外呼`、`WhatsApp`（下拉，绑定/管理 WhatsApp 坐席）。
- **新增 / 编辑成员弹窗**：
  - 姓名（必填）。
  - 登录邮箱（必填，需合法邮箱；限 `@dinoai.ai`，占位 `name@dinoai.ai`）。
  - 角色（必填，单选）。
  - 数据范围（业务线，多选）：角色数据范围为 `all` 时禁用并显示「全部业务线」；为 `line` 时**必填**。可选业务线含「其他」（勾选后可见归入"其他"的用户，见 5.3）。
  - 状态（启用 / 停用开关，默认启用）。
- 启用/停用切换、新增/编辑成员、绑定/解绑外呼坐席均写入操作日志。

#### 3.3.1 坐席绑定（外呼 + WhatsApp）

成员账号除基础信息外，承载该员工的**触达坐席能力**：一名员工需绑定坐席后，才能在销售中心发起外呼 / 通过 WhatsApp 触达。

**外呼坐席**
- 操作列「**绑定外呼 / 解绑外呼**」一键切换（对应账号字段 `outboundSeatBound`）；绑定/解绑写操作日志（`sys.log.bindOutbound` / `sys.log.unbindOutbound`），并提示「外呼坐席已绑定 / 已解绑」。
- 绑定后该员工可在销售中心使用点号码拨打（`sales_dial`）。落地时对接外呼供应商坐席（如 VoIP/Cloud PBX 的 DID 坐席）。

**WhatsApp 坐席**（成员列表展示以下 WA 列）
| 列 | 说明 |
| --- | --- |
| WA 账号 | 绑定的 WhatsApp 账号（未绑定为「—」） |
| WA 状态 | WhatsApp 连接状态（在线 / 离线 / 未绑定） |
| WA 最近在线 | 该 WhatsApp 坐席最近一次在线时间 |
| WA 最近同步 | 与 WhatsApp 最近一次数据同步时间 |
- 操作列「**WhatsApp**」下拉进行绑定 / 管理 WhatsApp 坐席；绑定后各 WA 列回填状态与时间。

#### 3.3.2 停用成员的影响（二次确认）

点击「停用」弹出确认弹窗（标题 Tips），明确影响后再执行：
- **该员工名下所有 Leads 将被释放并回归公海**，按分配规则重新分配给其他员工。
- **该员工将不再接收新的 Leads 分配**。
- 提示「如需修改信息，请点击『编辑』」。

确认后置为「停用」并写操作日志；「启用」则直接切换并写日志。停用是销售离职/休假时清空其线索、避免线索滞留的关键动作。

### 3.4 Tab 3 · 操作日志（审计）
- 字段：操作时间、操作人（邮箱）、操作行为、操作对象。
- 记录事件：新增/编辑/删除角色、新增/编辑成员、启用/停用成员、**绑定/解绑外呼坐席**。
- 分页展示（支持每页条数切换）。

### 3.5 功能需求清单（系统配置）
| 功能点 | 说明 | 权限点 | 优先级 |
| --- | --- | --- | --- |
| 角色列表 | 内置/预定标签、数据范围、成员数 | `system` / `system_role_edit` | P0 |
| 新增角色 | 名称+描述+数据范围+权限矩阵 | `system_role_add` | P0 |
| 编辑角色权限 | 权限矩阵按子权限细分，父子联动 | `system_role_edit` | P0 |
| 删除角色 | 内置/被占用不可删 | `system_role_delete` | P1 |
| 权限矩阵总览 | 模块×角色权限一览 | `system`(view) | P0 |
| 成员列表 | 角色、数据范围、状态、最近登录 | `system` | P0 |
| 新增/编辑成员 | 邮箱鉴权（@dinoai.ai）+ 角色 + 业务线范围 | `system_acc_add` / `system_acc_edit` | P0 |
| 启用/停用成员 | 停用需二次确认；停用后名下 Leads 释放回公海重分、不再接新分配 + 写日志 | `system_acc_edit` | P0 |
| 绑定/解绑外呼坐席 | 一键切换 `outboundSeatBound`，绑定后可发起外呼 + 写日志 | `system_acc_edit` | P1 |
| 绑定/管理 WhatsApp 坐席 | 绑定 WA 账号；列表展示 WA 账号/状态/最近在线/最近同步 | `system_acc_edit` | P1 |
| 操作日志 | 审计全部角色/成员/坐席变更 | `system`(view) | P1 |

---

## 4. 模块三：用户中心 · 添加会员（加有效期）

> 用户中心为一期模块；本节补充二期新增的「**添加会员 / 加有效期**」写操作能力（替代此前「找产研后台改库」），并纳入二期 RBAC（子权限 `users_add_member`）。

### 4.1 功能说明

用户中心列表**操作列新增「添加会员」按钮**。点击弹出弹窗，选择会员有效期「**开始时间 - 结束时间**」；确认后把该用户的**到期时间（`expireTime`）更新为所选结束时间**，并记录「最近修改人」、写入修改历史（字段：到期时间 修改前 → 修改后）。

### 4.2 有效期选择规则（按付费状态区分）

| 用户付费状态 | 开始时间 | 结束时间 |
| --- | --- | --- |
| **未付费**（未付费-未体验 / 体验中 / 已体验） | 无限制，可选任意日期 | 无限制 |
| **已付费**（付费 / 付费逾期） | **必须晚于当前「到期时间」**（只能从现有到期时间之后开始选，避免与已有有效期重叠 / 回退） | 无限制 |

- **权限**：需 `users_add_member`（用户中心「可操作」下的子权限）。
- **数据隔离**：仅可对数据权限范围（业务线）内的用户执行。
- **风控**：属高风险写操作（直接影响用户权益 / 收入口径），建议二次确认、单次时长上限、全量操作日志与可回溯；是否计入付费/续费统计、与订单/对账口径如何区分待确认（见 7）。

---

## 5. 权限与数据隔离（全局规则）

### 5.1 菜单可见性（二期）
| 模块 | 销售专员 | 销售组长 | 客服/运营 | 管理员 | 超管 |
| --- | --- | --- | --- | --- | --- |
| 销售中心 | 可操作（本人范围） | 可操作 + 设置/重分配 | 可查看/操作（视配置） | 可操作 | 全部 |
| 系统配置·角色权限 | 不可见 | 不可见 | 不可见 | 可操作 | 可操作 |

### 5.2 数据隔离原则
- **业务线隔离**：数据范围为 `line` 的账号仅见其分配业务线的数据；`all`（超管/管理员）见全业务线。业务线的判定口径见 **5.3**。
- **销售中心归属隔离**：非超管销售仅见本人领取的线索与本人外呼记录；超管 / 拥有 `sales_reassign` 的组长可见业务线范围内全部线索与通话记录。
- **登录鉴权**：仅 `@dinoai.ai` 邮箱可登录/注册。
- **兜底策略**：登录邮箱未匹配到成员账号时，原型按「超级管理员」处理（保证可用）；正式实现应改为按最小权限或拒绝访问，落地时确认。

### 5.3 业务线判定口径（数据权限归属）

**问题**：业务线是**配置渠道时创建**的，靠渠道归因绑定到用户。但 **App 直接注册、未走任何渠道**的用户不会绑定业务线，注册时只拿到**国家 IP**。若数据权限仅按"用户已绑定的业务线"过滤，这批无业务线用户会被"指定业务线"的员工全部漏掉（`matchLine('')` 恒为 false），造成**查不到**。

**判定规则（渠道优先 + 国家兜底，已定）**：任一用户的"所属业务线"按以下优先级解析，权限过滤、筛选器、销售中心归属统一使用**解析后的业务线**：
1. **有渠道码**（落地页/KOL 投放）→ 按渠道关联的业务线归属，员工可见其数据权限对应业务线下的用户。
2. **无渠道码**（App 直投）→ 按**注册国家 IP 映射业务线**：查找"国家清单包含该国家码"的业务线并归入之。
3. **映射不上** → 统一归 **「其他」**业务线（含空白 / `ZZ` / 未知），确保**不产生无归属孤儿**；数据范围为 `all` 的角色始终可见全部用户。

**数据权限内置业务线（8 条，已定）** 及国家映射：国家以 **ISO 3166-1 alpha-2 国家码**为准（与用户注册国家 IP 字段一致）；映射关系**随业务线配置一起维护**，每条业务线可绑定**一个或多个国家码**，且配置项需**支持全量国家码**（实际用户已覆盖 50+ 国）。

| 业务线 | 绑定国家码（ISO alpha-2，可多国） | 电话国家码 |
| --- | --- | --- |
| 马来 | MY | +60 |
| 泰国 | TH | +66 |
| 印尼 | ID | +62 |
| 新加坡 | SG | +65 |
| 越南 | VN | +84 |
| 韩国 | KR | +82 |
| 沙特 | SA | +966 |
| 其他 | 未被上述业务线绑定的其余全部国家码（US/FR/EG/HK/DE/CN/IN/JP… 及空白、`ZZ`/未知）—— catch-all | 多国 |

> **实测国家分布（约 1.2 万用户，供配置参考）**：ID 3507、MY 2561、VN 2244、SA 1557、TH 1150、KR 528、SG 39（以上均已单列业务线）；其余进入"其他"：US 89、FR 54、EG 30、HK 28、DE 23、CN 21、IN…（长尾 40+ 国，多为个位数）+ 空白 12、`ZZ` 3。

**"其他"用户的可见性（已定）**："其他"是一条**正常可选的业务线**。在系统配置 → 角色/成员的**数据权限**中勾选了"其他"的员工，即可见归入"其他"的用户；未勾选则不可见（`all` 全部业务线角色仍可见）。因此"其他"须作为业务线出现在数据权限的可选项中。

**效果**：Korea IP 直投 App 的用户会落入"韩国"业务线，被数据范围含"韩国"的员工正常查询到；无法归类的用户落入"其他"，由数据权限含"其他"的员工负责查看。

**业务线解析算法（`businessLineOf(channels, user)`，已实现）**：所有权限过滤、筛选器、销售中心归属统一调用此函数得到用户的所属业务线，按以下顺序判定：

```
1. 有渠道 code：
   channelLineByCode(channels, code) —— 遍历渠道树，找到该 code 所在节点，
   回溯到其【顶层业务线】并返回（精确关联）。命中则返回该业务线。
2. 无渠道 code（或 code 未命中）+ 有注册国家：
   先做别名归一（如「马来西亚」→「马来」）；
   若归一后的国家命中业务线枚举（且非「其他」）→ 返回该业务线；
   否则 → 返回「其他」。
3. 两者都取不到 → 兜底返回「其他」（不再产生空业务线）。
```

- 关键变化：`businessLineOf()` 不再返回空字符串，无法归类一律落「其他」，因此不会再被 `matchLine('')` 漏掉。
- 币种与电话区号已为新业务线补齐：**泰国 THB / +66**、**新加坡 SGD / +65**（连同 `LINE_CURRENCY`、`COUNTRY_CODE` 映射）。
- 数据权限的业务线可选项包含全部 8 条（含「其他」）。
- 说明：原型 `country` 字段以国家名/别名匹配业务线（`马来西亚→马来`）；正式实现以 **ISO alpha-2 国家码 → 业务线**为准（见上表），口径一致。
- 历史数据可在刷库（2.8）时一并按国家回填业务线。

---

## 6. 数据结构摘要

- `Student.salesOwner / salesProgress / salesLatestNote / salesUpdatedAt / salesHistory[] / purchaseIntent`：销售中心跟进字段（`salesProgress`：待领取 / 跟进中 / 暂不跟进；`purchaseIntent`：未填写 / 有意向 / 无意向，默认未填写，由外呼小结弹窗写入）。
- `Student.expireTime`：会员到期时间；用户中心「添加会员」（4.2）确认后更新为所选结束时间，并写入修改历史。
- `SalesFollowLog { progress, note, time, owner, audioUrl? }`：单条跟进记录（含录音；`owner` 可为「系统」表示自动动作）。
- `CallRecord { id, studentId, customer, phone, businessLine, result, duration, note, agent, time }`：外呼记录（`businessLine` 存储用于筛选/隔离，不作展示列）。
- `SalesSettings { autoDropEnabled, autoDropMinutes, allocations[{email,weight}] }`：分配/掉库设置；平台状态中为 **`Record<业务线, SalesSettings>`（按业务线独立配置）**。
- `BusinessLine`（数据权限内置 8 条）：马来 / 泰国 / 印尼 / 新加坡 / 越南 / 韩国 / 沙特 / 其他。
- `Role { id, name, desc, builtin, planned?, dataScope, perms:Record<ModuleKey,PermLevel> }`：角色。
- `Account { id, email, name, roleId, businessLines[], status, lastLogin?, salesLead?, outboundSeatBound? }`：成员账号（`outboundSeatBound` 是否已绑定外呼坐席；WhatsApp 坐席相关 WA 账号/状态/最近在线/最近同步为触达坐席字段）。
- `AuditLog { id, time, actor, module, action, target? }`：操作日志。

---

## 7. 待确认事项（Open Issues）
1. **外呼接入方案**：第三方外呼平台选型、点号码拨打的对接方式、通话录音回传与存储、AI 通话小结生成能力（是否真调用 AI）。该方案是二期排期与实现范围确认的关键依赖。
2. **自动分配 / 掉库的执行时机与算法**：原型在数据加载/改设置时以 hash 模拟随机分配；正式需明确后端定时任务频率、按权重随机分配算法、并发与幂等处理。
3. **30 天退回公海阈值**：30 天阈值是否需可配置；退回公海后是否通知原销售（当前逻辑：仅触发一次，被捞取后免疫自动掉库）。
4. **登录兜底权限**：正式环境未匹配账号的用户应采用最小权限还是拒绝访问（原型为超管兜底）。
5. **转化闭环触发源**：付费事件如何实时回传并触发线索离场（订单/支付真实事件对接）。
6. **测试线索剔除**：用户类型「测试用户」是否在销售中心（待领取/自动分配）中默认排除。
7. **历史数据刷库口径**：手机号规整规则（国家码/空格/分隔符）、一号对多用户的处理、绑定后是否通知销售、以及映射表交付时间（影响上线切换）。
8. **业务线国家绑定范围**：数据权限内置 8 条业务线（马来/泰国/印尼/新加坡/越南/韩国/沙特/其他）；国家码采用 ISO 3166-1 alpha-2 全量清单（实测覆盖 50+ 国），未绑定国家 + 空白/`ZZ`/未知归"其他"。
9. **坐席对接方案**：外呼坐席与外呼供应商（VoIP/Cloud PBX，见马来 WEB ASP 合同）的账号开通/绑定方式；WhatsApp 坐席的接入方式（WhatsApp Business API / 第三方）、账号状态与在线/同步机制；停用员工时坐席是否随之释放。
10. **添加会员（加有效期）口径**：开放对象（销售/运营/仅管理员）、是否需二次确认/审批与单次时长上限；手动加的会员是否计入付费/续费统计、与订单/对账如何区分；是否需回写真实会员系统或仅改 CRM 展示。

---

## 8. 关联资源
- 原型仓库：https://github.com/jiangshuang12345-tech/CRM.git
- 在线原型：https://jiangshuang12345-tech.github.io/CRM/
- 关键原型源码：`src/pages/SalesCenter.tsx`、`src/pages/SystemConfig.tsx`、`src/funnel.ts`、`src/perm.ts`、`src/types.ts`
- 技术栈：Vite + React + TypeScript + Ant Design 5（纯前端 Mock，数据存 localStorage）
