# Dino English CRM 第三期需求文档（PRD）

## 1. 需求概述

### 1.1 背景与目标

销售在谈单时需要快速了解用户的学习情况与购买记录，但当前用户报告、回放和主子订单信息分散，主子订单展示也不清晰，且试听课级别无法由一线人员及时调整，影响销售判断、沟通和后续试听安排。同时，部分业务场景需要将外部 Leads 导入销售中心处理。

第三期目标是补齐销售谈单所需的用户、订单与线索信息，让销售能够：

- 在用户中心和销售中心查看用户详情，包括上课记录、报告与回放；
- 由具备权限的销售或运营手动修改用户的试听课级别，并可追溯操作记录；
- 在订单中心查看主订单及其下全部子订单流水，快速理解完整交易链路；
- 通过销售中心的 Leads 导入入口兼容更多业务场景。

### 1.2 范围与优先级

| 模块 | 能力 | 价值 | 优先级 |
| --- | --- | --- | --- |
| 用户中心 | 用户详情：上课记录、报告、回放 | 为销售谈单提供完整学习信息 | P0 |
| 用户中心 / 销售中心 | 修改试听课级别 | 让销售或运营可及时纠正、调整试听课级别 | P0 |
| 订单中心 | 订单详情：主订单及全部子订单流水 | 解决主子订单展示混乱，便于核对交易 | P1 |
| 销售中心 | 用户详情：上课记录、报告、回放 | 在跟进线索时无需跨模块查找学习信息 | P0 |
| 销售中心 | Facebook Lead Form 自动导入与自动注册 | 实时将 Facebook 线索注册或关联为 Dino 用户后进入销售闭环 | P0 |
| 销售中心 | Leads 导入入口 | 兼容更多业务场景的线索进入与分配 | P2 |
| 系统配置 | 三期模块权限管理 | 控制三期菜单、详情路由及操作入口 | P0 |

---

## 2. 用户中心：用户详情

### 2.1 页面目标

用户详情页作为用户学习信息的统一入口，供销售在谈单前或沟通中快速判断用户的学习进展与体验情况。

### 2.2 展示内容

| 区块 | 展示内容 |
| --- | --- |
| 基本信息 | 用户基础资料与当前用户状态 |
| 上课记录 | 用户课程的课标、课程状态、课程名称、老师、上课时间与完课时间 |
| 报告 | 与课程对应的 Trial Report / Lesson Report，支持查看 |
| 回放 | 有回放的课程展示回放入口，支持进入播放 |

### 2.3 上课记录列表字段

上课记录列表字段按以下顺序展示：

| 字段 | 说明 |
| --- | --- |
| 课标 | 课程课次唯一标识 |
| 课程状态 | 在「课标」后新增；展示已预约 / 进行中 / 已完课 / 已取消等课程状态 |
| 课程名称 | 原「课程类型」字段调整为「课程名称」，展示具体课程名称 |
| 老师 | 展示授课老师姓名，如 Kim、Max；无数据时展示「—」 |
| 上课时间 | 原列表中的「完课时间」字段改名为「上课时间」，展示课程实际/计划上课时间 |
| 完课时间 | 新增字段；课程已完课时展示实际完课时间，进行中课程展示「—」 |
| 报告 | 与对应课次关联；有报告时提供查看入口，无则展示「—」 |
| 回放 | 与对应课次关联；有回放时提供播放入口，无则展示「—」 |

### 2.4 关键规则

- 用户详情仅展示当前账号数据权限范围内的用户信息。
- 上课记录不再只展示已完课课程，需包含进行中课程；具体可见状态范围以课程状态枚举为准。
- 上课记录按上课时间倒序展示；上课时间相同时按课程记录创建时间倒序。
- 进行中课程的完课时间固定展示「—」，不得用上课时间或预计结束时间代替。
- 报告、回放均与对应课次关联；未完课课程无报告或回放时展示「—」。
- 无报告或无回放时明确展示为空，避免销售误判为数据缺失。

### 2.5 修改试听课级别

销售或运营可在用户中心列表中手动修改用户的**试听课级别**，用于处理级别识别不准确或业务沟通后需要调整的场景。

| 功能点 | 规则 |
| --- | --- |
| 展示字段 | 用户中心列表及用户详情展示当前「课程等级」；无值时展示「—」。 |
| 操作入口 | 用户中心三期列表「操作」下拉菜单新增「修改试听课等级」。仅未付费用户展示该菜单项。 |
| 弹窗标题 | `修改试听课等级 · {用户ID}`。 |
| 修改方式 | 「试听课等级」为必填下拉框，选项固定为 `L1 / L2 / L3 / L4 / L5`，不支持自由输入。打开弹窗时回显当前等级。 |
| 可操作权限 | 沿用用户中心三期「修改用户」权限，即 `usersV2_edit=operate`；只读或无权限账号不展示入口，接口同时拒绝提交。 |
| 数据范围 | 仅可修改当前账号业务线数据权限范围内的用户。 |
| 保存结果 | 保存成功后更新用户 `courseLevel`，关闭弹窗并提示「试听课等级已修改」。 |
| 修改历史 | 等级发生变化时，用户修改历史新增一条「试听课等级：修改前 → 修改后」，记录操作人和操作时间，并更新最近修改人；等级未变化时不新增历史。 |
| 影响边界 | 仅修改用户当前试听课等级，不改写已生成的上课记录、报告、回放或正式课等级。 |

> 用户不存在或已超出当前账号的数据范围时，接口拒绝操作且不泄露用户信息。原型当前不要求填写修改原因，也不增加额外二次确认。

---

## 3. 订单中心：订单详情与主子订单流水

### 3.1 页面目标

统一展示主订单与子订单之间的关系，解决当前主子订单展示混乱的问题，让销售和运营能快速完成订单核对。

### 3.2 展示内容

| 区块 | 展示内容 |
| --- | --- |
| 主订单信息 | 主订单号、用户、商品、订单状态、支付金额、支付时间等 |
| 子订单流水 | 当前主订单下的所有子订单；每条展示子订单号、商品/权益、金额、状态、创建与支付时间等流水信息 |
| 汇总信息 | 主订单下子订单数量与金额汇总，便于快速核对 |

### 3.3 关键规则

- 订单详情以主订单为聚合入口，完整展示其下所有子订单流水。
- 子订单按创建或支付时间排序，并保留各自独立状态。
- 用户只能查看其数据权限范围内的订单及相关子订单。

---

## 4. 销售中心：用户详情与 Leads 导入

### 4.1 销售中心用户详情

销售在跟进线索时，可直接进入用户详情查看上课记录、报告与回放，无需离开销售工作台到其他模块重复检索。

> **课程范围说明**：销售中心只承接「付费前」线索，因此销售中心用户详情**仅展示体验课记录**（包含进行中和已完课等状态），不展示正式课。用户完成付费后离开销售中心，正式课记录在用户中心详情中查看。

| 功能点 | 说明 |
| --- | --- |
| 用户详情入口 | 「待领取注册用户」「我的跟进」列表中的用户ID可点击；跟进详情中同步提供入口 |
| 基本信息 | 展示用户ID、姓名、状态、年龄段、登录账号、国家、业务线、渠道、注册时间及会员到期时间 |
| 上课记录 | 仅展示该用户的体验课，字段与用户中心一致：课标、课程状态、课程名称、老师、上课时间、完课时间、报告、回放；按上课时间倒序，不展示正式课 |
| 修改试听课级别 | 在「我的跟进」列表的「更多」菜单中新增「修改试听课等级」；仅具备 `salesV3_update=operate` 且在线索可见范围内的账号可操作 |
| 课程报告 | 仅展示体验课对应的 Trial Report；销售中心不展示 Lesson Report |
| 课程回放 | 体验课有回放链接时展示「回放」入口并在新标签页播放；无回放时展示「—」 |
| 返回交互 | 返回销售中心原 Tab，并保留进入详情前的筛选条件和分页位置 |
| 数据权限 | 沿用销售中心、业务线及线索归属规则，不因详情入口扩大可见范围 |

**权限与边界**

- 普通销售只能查看本人可见或已领取线索的用户详情。
- 销售组长或拥有重新分配权限的账号，可查看其业务线范围内的线索详情。
- 待领取线索仅限有权访问对应待领取池的账号查看详情。
- 销售中心详情接口应按「体验课」过滤课程数据，即使底层存在异常关联的正式课记录也不得返回或展示；进行中体验课需正常展示，完课时间为「—」。
- 用户不存在或超出可见范围时，统一提示「未找到该用户，或你没有访问权限」，不得泄露用户是否存在。
- 销售中心用户详情为只读能力，本期不支持在详情页修改课程记录、报告或回放。
- 修改试听课等级弹窗回显当前等级，必选 `L1 / L2 / L3 / L4 / L5` 之一；保存后更新用户课程等级及销售线索最后更新时间，并提示修改成功。
- 修改试听课等级仅改变等级字段，不新增销售跟进备注，不改变线索进度、归属、课程记录、报告或回放。
- 用户完成付费并离开销售中心后，不得利用历史详情链接绕过用户中心权限继续访问；是否为原归属销售保留历史查看权限待确认。

### 4.2 Leads 导入入口

销售中心新增 Leads 导入入口，以兼容更多业务场景的线索进入 CRM 后再进行分配、跟进与外呼。

| 功能点 | 说明 |
| --- | --- |
| 导入入口 | 在销售中心提供明确的 Leads 导入入口 |
| 导入后流转 | 导入成功的 Leads 按销售中心规则进入分配 / 跟进流程 |
| 权限控制 | 仅具备相应销售中心操作权限的角色可使用该入口 |

> 导入模板字段、重复 Leads 处理、错误校验与历史数据迁移规则，待业务场景进一步确认后补充。

### 4.3 Facebook Lead Form 自动导入与自动注册

Facebook Lead Ads 用户在 Facebook 内提交 Lead Form 后，无需跳转 Dino English 落地页。系统通过 Meta 官方 Lead Ads Webhook 接收事件，并使用 Graph API 拉取表单明细，实时将线索接入 Dino English 与 CRM 销售中心。

**目标：** 消除人工下载、整理和导入的时延；让 Facebook 新线索自动完成用户关联或注册，并直接进入既有的销售分配、跟进与外呼闭环。

| 功能点 | 规则 |
| --- | --- |
| 实时接入 | Meta Lead Ads Webhook 收到提交事件后，调用 Graph API 获取 Lead Form 明细并写入 CRM；支持 Webhook 重试，不得因重复回调重复创建线索。 |
| 用户匹配与注册 | 先按规范化手机号匹配 Dino English 用户；已存在则关联该用户，不存在则调用既有自动注册服务创建用户（不新增短信验证码流程）。 |
| 线索流转 | 成功关联或注册后，Lead 写入销售中心，按现有自动分配、领取、跟进与外呼规则流转。 |
| 投放归因 | 保存并关联 Facebook Campaign、Ad Set、Ad、Form 及 Meta Lead ID，支持后续与 Dino 用户、订单和付费数据稳定关联。 |
| 异常处理 | 手机号缺失、格式不合法、Graph API 获取失败或写入失败时进入异常队列，可重试、可追踪；异常线索不得静默丢失。 |

**处理流程：**

`Facebook Lead Form 提交 → Meta Webhook → Graph API 获取表单数据 → 手机号规范化/校验 → 匹配已有 Dino 用户 或 调用既有自动注册服务 → 写入 CRM 销售线索 → 自动分配 / 跟进 / 外呼`

**去重与数据规则：**

- 以 Meta Lead ID 作为外部事件幂等键；同一提交事件重复推送时只处理一次。
- 手机号匹配须先完成国家码、空格及符号等格式规范化；匹配到已有用户时不得重复注册。
- 表单原始字段、同步状态、失败原因及重试记录须可追溯；销售侧仅使用已成功入库的线索。
- Facebook 渠道标识与 Campaign、Ad Set、Ad、Form 信息随 Lead 保留，用于后续转化与付费归因。

---

## 5. 系统配置：三期权限管理

### 5.1 权限矩阵新增项

系统配置 → 角色权限的权限矩阵新增以下三个主模块权限项，并在名称右侧展示灰色「**三期**」版本标签，以便与一期、二期模块区分：

| 权限项 | 建议权限 Key | 控制范围 |
| --- | --- | --- |
| 用户中心（三期） | `users_phase3` | 三期用户中心菜单、用户详情、上课记录、Trial/Lesson Report、课程回放及试听课等级展示 |
| 订单中心（三期） | `orders_phase3` | 三期订单中心菜单、订单详情、主订单及全部子订单流水 |
| 销售中心（三期） | `sales_phase3` | 三期销售中心菜单、销售线索用户详情、体验课报告/回放、Leads 导入及 Facebook Lead Form 同步结果查看 |

「修改试听课等级」不新增独立权限项，复用对应模块已有写权限：

- 用户中心三期：`usersV2_edit=operate`。
- 销售中心三期：`salesV3_update=operate`。

- 三项权限均支持「无权限 / 只读 / 可操作」。
- 三期权限作为独立权限项保存，不因同名一期/二期模块已有权限而自动获得；避免新功能上线后扩大现有角色权限。
- 角色的数据范围仍复用现有 `all`（全部业务线）或 `line`（指定业务线），不为三期重复配置业务线。
- 新增/编辑角色时均可配置三期权限；权限变更需写入操作日志。

### 5.2 权限级别行为

| 权限项 | 无权限 | 只读 | 可操作 |
| --- | --- | --- | --- |
| 用户中心（三期） | 菜单隐藏；列表和详情路由不可访问 | 可查看列表、用户详情、上课记录、报告及回放；不可修改试听课等级 | 模块可操作且 `usersV2_edit=operate` 时，可修改未付费用户的试听课等级 |
| 订单中心（三期） | 菜单隐藏；列表和详情路由不可访问 | 可查看订单列表、订单详情及全部子订单流水 | 当前三期能力均为查看，效果与只读一致；保留层级供后续写操作扩展 |
| 销售中心（三期） | 菜单隐藏；列表、线索详情及导入接口不可访问 | 可查看权限范围内的销售列表、用户详情及 Facebook 同步结果；隐藏试听课等级修改、Leads 导入及其他写操作 | 可查看用户详情、Facebook 同步结果，并使用 Leads 导入；`salesV3_update=operate` 时可修改可见线索的试听课等级，其他操作仍需对应二期子权限 |

> 「可操作」不代表自动拥有所有二期销售子权限。例如角色拥有 `sales_phase3=operate` 但没有 `sales_dial`，仍不可外呼；拥有 `sales_phase3=view` 时，无论二期是否有导入类操作权限，三期 Leads 导入入口均不可用。

### 5.3 菜单、路由与按钮联动

- **菜单**：对应权限为 `none` 时隐藏该三期菜单；为 `view` 或 `operate` 时显示。
- **列表路由**：直接输入三期列表地址时必须重新校验模块权限，无权限返回 403/无权限页，不得仅依赖菜单隐藏。
- **详情路由**：用户详情和订单详情同时校验三期模块权限与业务线数据范围；销售用户详情还需校验线索归属/可见范围。
- **报告与回放**：视为详情查看能力，`view` 与 `operate` 均可使用；报告、回放接口继续执行相同的数据权限校验。
- **修改试听课等级**：用户中心仅在 `usersV2_edit=operate` 且用户为未付费状态时展示；销售中心仅在 `salesV3_update=operate` 且线索位于账号可见范围时展示。服务端须再次校验模块权限、业务线及销售线索归属。
- **Leads 导入按钮**：仅 `sales_phase3=operate` 时展示并允许调用导入接口；`view` 或 `none` 均拒绝。Facebook Lead Form 同步由系统自动执行，`view` 与 `operate` 可在各自数据范围内查看同步结果。
- **已有写操作**：销售领取、更新跟进、外呼、重新分配和设置按钮，采用「三期模块为可操作 + 对应二期子权限允许」的交集判断。
- **权限即时生效**：角色权限保存后，成员下次请求即按新权限判断；已打开页面在接口返回无权限时跳转无权限页，不继续展示缓存中的敏感数据。

### 5.4 权限继承与初始化

- 超级管理员默认三项均为「可操作」。
- 其他存量角色升级后三项默认「无权限」，由管理员按岗位重新授权，防止默认放大数据访问范围。
- 一个成员同时拥有多个角色时，沿用系统现有角色合并规则；若当前系统不支持多角色，则以成员绑定的唯一角色为准。
- 三期权限仅控制三期菜单和路由，不改变一期/二期同名模块的既有权限。
- 成员被停用后，无论角色配置为何，所有三期菜单、路由和接口均不可访问。

### 5.5 权限配置验收矩阵

| 场景 | 预期结果 |
| --- | --- |
| 三期模块为无权限 | 菜单不可见；直接访问列表/详情地址及接口均被拒绝 |
| 用户/订单三期为只读 | 可进入详情、查看报告/回放或流水，不出现写操作 |
| 销售三期为只读 | 可查看本人权限范围内线索详情；不展示 Leads 导入及其他操作按钮 |
| 销售三期为可操作，无对应二期子权限 | 可使用 Leads 导入；领取、外呼、重分配等仍不可用 |
| 销售三期为可操作，有对应二期子权限 | 对应领取、跟进、外呼、重分配等按钮按子权限正常展示 |
| 用户中心账号具备 `usersV2_edit=operate` | 未付费用户的操作菜单展示「修改试听课等级」；保存后修改历史可查修改前后、操作人和时间 |
| 销售中心账号具备 `salesV3_update=operate` | 「我的跟进」中可见线索的“更多”菜单展示「修改试听课等级」；不可修改范围外线索 |
| 有模块权限但超出业务线/归属范围 | 详情及接口仍拒绝访问，不泄露数据是否存在 |
| 管理员修改角色三期权限 | 权限保存成功、成员请求即时生效，并生成操作日志 |

---

## 6. 验收标准

1. 销售可从销售中心待领取列表、我的跟进列表或跟进详情进入用户详情，查看基本信息及体验课记录；列表依次展示课标、课程状态、课程名称、老师、上课时间、完课时间、报告和回放，进行中课程的完课时间展示「—」；不得展示正式课或 Lesson Report；返回后保留原 Tab、筛选条件和分页位置。
2. 用户中心具备 `usersV2_edit=operate` 的账号可修改数据范围内未付费用户的试听课等级；销售中心具备 `salesV3_update=operate` 的账号可修改「我的跟进」中可见线索的试听课等级。下拉仅支持 L1–L5，保存后等级正确更新，且不影响既有课程记录、报告、回放及正式课等级。
3. 订单详情可按主订单完整展示其下所有子订单流水，主子关系与金额信息清晰可核对。
4. 销售中心提供 Leads 导入入口；成功导入后线索可进入既有分配、跟进与外呼流程。
5. Facebook Lead Form 提交后，系统可通过 Meta Webhook 与 Graph API 实时同步线索；按手机号关联已有 Dino 用户或调用既有自动注册服务创建用户，再纳入销售中心既有分配、跟进与外呼流程。
6. 同一 Meta Lead ID 的重复回调不会生成重复 Lead；手机号格式差异不会生成重复用户；同步失败可查看失败原因并重试，线索不会静默丢失。
7. Campaign、Ad Set、Ad、Form 与 Meta Lead ID 可随线索保存并关联到 Dino 用户，供后续订单及付费归因使用。
8. 用户、订单、线索及导入入口均遵守现有角色与业务线数据权限。
9. 普通销售无法通过直接输入详情地址查看本人不可见的线索；越权与用户不存在使用统一提示。
10. 权限管理中展示带灰色「三期」标识的用户中心、订单中心、销售中心权限项，均可配置无权限/只读/可操作。
11. 三期权限与菜单、列表路由、详情路由、报告/回放、试听课等级修改、Leads 导入按钮及 Facebook 同步结果正确联动，前端隐藏与接口鉴权结果一致；用户中心修改历史正确记录等级变化。
